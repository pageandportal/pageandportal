{
  "name": "pageandportal",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync:content": "node scripts/sync-content.mjs",
    "sync:assets": "node scripts/sync-assets.mjs",
    "sync": "npm run sync:content && npm run sync:assets"
  },
  "dependencies": {
    "astro": "^6.3.7"
  }
}
